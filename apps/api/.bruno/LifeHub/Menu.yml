info:
  name: Menu
  type: http
  seq: 1

http:
  method: GET
  url: "{{process.env.API_URL}}/dashboard/menu"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
