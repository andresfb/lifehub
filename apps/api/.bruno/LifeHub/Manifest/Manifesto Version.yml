info:
  name: Manifesto Version
  type: http
  seq: 1

http:
  method: GET
  url: "{{process.env.API_URL}}/manifesto/version"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
