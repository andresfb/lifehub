info:
  name: Logout
  type: http
  seq: 5

http:
  method: POST
  url: "{{process.env.API_URL}}/logout"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
