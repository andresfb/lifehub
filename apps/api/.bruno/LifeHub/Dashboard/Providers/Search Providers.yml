info:
  name: Search Providers
  type: http
  seq: 1

http:
  method: GET
  url: "{{process.env.API_URL}}/dashboard/search/providers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
