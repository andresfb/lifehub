info:
  name: Pins List
  type: http
  seq: 1

http:
  method: GET
  url: "{{process.env.API_URL}}/dashboard/pins"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
