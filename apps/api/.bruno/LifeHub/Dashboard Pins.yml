info:
  name: Dashboard Pins
  type: http
  seq: 3

http:
  method: GET
  url: "{{process.env.API_URL}}/dashboard/pins"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
