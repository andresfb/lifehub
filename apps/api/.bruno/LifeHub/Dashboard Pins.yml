info:
  name: Dashboard Pins
  type: http
  seq: 2

http:
  method: GET
  url: "{{process.env.API_URL}}/dashboard/pins?include=items,user"
  params:
    - name: include
      value: items,user
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
